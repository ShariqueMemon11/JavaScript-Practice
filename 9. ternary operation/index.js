// ternary operator = a shortcut to if{} & else statements
// helps to assign a veriable based on conditio

let time = 9;

let greatings = time < 12 ? `good Morning` : `Good Afternoon`

console.log(greatings)